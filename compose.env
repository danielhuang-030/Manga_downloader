# 僅供 `docker compose` 做專案變數替換（勿放含 `$` 的 MANGA_COOKIES，否則 Compose 會把 `$xxx` 當成變數）。
# 執行範例：docker compose --env-file compose.env run --rm python python -m pytest tests/ -v
# 應用程式仍透過 volume 讀取專案根目錄的 `.env`（python-dotenv），與本檔無關。
COMPOSE_PROJECT_NAME=mangadw
